{
  "$schema": "http://json-schema.org/schema",
  "id": "CardSchema",
  "title": "Card design 1",
  "type": "object",
  "properties": {
    "name": {
      "description": "Specifies the name of the generated Class.",
      "type": "string",
      "$default": {
        "$source": "argv",
        "index": 0
      }
    }
  },
  "required": ["name"]
}
